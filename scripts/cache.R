.cache_env <- new.env(parent = emptyenv())

#.cache_env <- NULL  # Declare at top-level

#.onLoad <- function(libname, pkgname) {
  # Initialize the environment when the package is loaded
#  .cache_env <<- new.env(parent = emptyenv())
#}
